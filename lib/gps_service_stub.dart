/// Web: GPS przez geolocator niedostępny — zwracamy null.
/// Użytkownik może skorzystać z przycisku pin_drop (wpisz współrzędne).
Future<({double lat, double lon})?> getCurrentGpsLocation() async => null;